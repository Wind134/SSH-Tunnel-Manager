<Application x:Class="SSHTunnelManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Theme brushes. Swapped at runtime by ThemeHelper for Light/Dark.
                 Referenced via DynamicResource so they re-evaluate on change. -->
            <SolidColorBrush x:Key="AppBg" Color="WhiteSmoke" />
            <SolidColorBrush x:Key="AppFg" Color="Black" />
            <SolidColorBrush x:Key="PanelBg" Color="#F5F5F5" />
            <SolidColorBrush x:Key="BorderCol" Color="LightGray" />
            <SolidColorBrush x:Key="MutedFg" Color="#555555" />

            <Style TargetType="Button">
                <Setter Property="MinWidth" Value="60" />
                <Setter Property="Cursor" Value="Hand" />
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
