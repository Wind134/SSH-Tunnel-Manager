<Window x:Class="SSHTunnelManager.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Width="400" Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBg}"
        TextElement.Foreground="{DynamicResource AppFg}"
        TextElement.FontFamily="Segoe UI">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <CheckBox Grid.Row="0" x:Name="AutoStartMinimizedBox"
                  Content="启动时最小化到任务栏"
                  Margin="0,4,0,4" />

        <CheckBox Grid.Row="1" x:Name="MinimizeToTrayBox"
                  Content="关闭时最小化到托盘（后台继续运行）"
                  Margin="0,4,0,4" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4,0,4">
            <TextBlock Text="主题：" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox x:Name="ThemeBox" Width="140">
                <ComboBoxItem Content="跟随系统" Tag="System" />
                <ComboBoxItem Content="浅色" Tag="Light" />
                <ComboBoxItem Content="深色" Tag="Dark" />
            </ComboBox>
        </StackPanel>

        <TextBlock Grid.Row="3" FontSize="11" Opacity="0.6"
                   TextWrapping="Wrap"
                   Text="选择“深色”或“浅色”会立即生效；选择“跟随系统”将使用 Windows 的应用主题。" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click" />
            <Button Content="取消" Width="80" IsCancel="True" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
