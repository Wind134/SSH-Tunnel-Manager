<Page
    x:Class="TinyTools.WinUI.Pages.FileLocksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="32" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" /><RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="文件和文件夹占用" Style="{StaticResource TitleTextBlockStyle}" />
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBox x:Name="PathBox" PlaceholderText="输入文件或文件夹完整路径" />
            <Button x:Name="QueryButton" Grid.Column="1" Content="查询" Click="QueryButton_Click" />
        </Grid>
        <TextBlock x:Name="QueryStatus" Grid.Row="2" Opacity="0.7" TextWrapping="Wrap" />
        <ListView x:Name="LockList" Grid.Row="3" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12,8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="80" /><ColumnDefinition Width="180" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Pid}" />
                        <TextBlock Grid.Column="1" Text="{Binding ProcessName}" />
                        <TextBlock Grid.Column="2" Text="{Binding ProcessPath}" TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
