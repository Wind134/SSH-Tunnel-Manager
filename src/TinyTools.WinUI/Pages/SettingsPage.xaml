<Page
    x:Class="TinyTools.WinUI.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Padding="32" Spacing="18" MaxWidth="720" HorizontalAlignment="Left">
        <TextBlock Text="设置" Style="{StaticResource TitleTextBlockStyle}" />
        <TextBlock Text="外观" Style="{StaticResource SubtitleTextBlockStyle}" />
        <ComboBox x:Name="ThemeBox" Header="应用主题" Width="280" HorizontalAlignment="Left"
                  SelectionChanged="ThemeBox_SelectionChanged">
            <ComboBoxItem Content="跟随系统" Tag="System" />
            <ComboBoxItem Content="浅色" Tag="Light" />
            <ComboBoxItem Content="深色" Tag="Dark" />
        </ComboBox>
        <InfoBar IsOpen="True" IsClosable="False" Severity="Informational"
                 Message="托盘、单实例、通知与其余设置将在后续阶段迁移；WPF 版本继续作为回退。" />
    </StackPanel>
</Page>
