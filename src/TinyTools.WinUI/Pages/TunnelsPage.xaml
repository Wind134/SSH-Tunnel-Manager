<Page
    x:Class="TinyTools.WinUI.Pages.TunnelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="SSH 隧道" Style="{StaticResource TitleTextBlockStyle}" />
        <InfoBar Grid.Row="1" IsOpen="True" IsClosable="False" Severity="Informational"
                 Message="当前阶段只读展示既有配置；连接、编辑和 Host Key 对话框将在下一阶段接入。" />
        <ListView x:Name="TunnelList" Grid.Row="2" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="16" Margin="0,0,0,8" CornerRadius="8"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="{Binding MaskedHost}" Opacity="0.7" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding LocalPort}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{Binding RemotePort}" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
