<Page
    x:Class="TinyTools.WinUI.Pages.PortsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="32" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock Text="端口与进程" Style="{StaticResource TitleTextBlockStyle}" />
            <Button x:Name="RefreshButton" Grid.Column="1" Content="刷新" Click="RefreshButton_Click" />
        </Grid>
        <InfoBar Grid.Row="1" IsOpen="True" IsClosable="False" Severity="Informational"
                 Message="POC 结论：使用原生虚拟化 ListView + 固定列布局，避免引入 2021 年后未更新的 DataGrid。" />
        <Grid Grid.Row="2" Padding="12,8" ColumnSpacing="12" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" /><ColumnDefinition Width="150" /><ColumnDefinition Width="80" />
                <ColumnDefinition Width="180" /><ColumnDefinition Width="80" /><ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="PID" Style="{StaticResource CaptionTextBlockStyle}" />
            <TextBlock Grid.Column="1" Text="进程" Style="{StaticResource CaptionTextBlockStyle}" />
            <TextBlock Grid.Column="2" Text="协议" Style="{StaticResource CaptionTextBlockStyle}" />
            <TextBlock Grid.Column="3" Text="本地地址" Style="{StaticResource CaptionTextBlockStyle}" />
            <TextBlock Grid.Column="4" Text="端口" Style="{StaticResource CaptionTextBlockStyle}" />
            <TextBlock Grid.Column="5" Text="状态 / 远端" Style="{StaticResource CaptionTextBlockStyle}" />
        </Grid>
        <ListView x:Name="PortList" Grid.Row="3" SelectionMode="Single">
            <ListView.ItemsPanel><ItemsPanelTemplate><ItemsStackPanel Orientation="Vertical" /></ItemsPanelTemplate></ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12,8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70" /><ColumnDefinition Width="150" /><ColumnDefinition Width="80" />
                            <ColumnDefinition Width="180" /><ColumnDefinition Width="80" /><ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Pid}" />
                        <TextBlock Grid.Column="1" Text="{Binding ProcessName}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="2" Text="{Binding Family}" />
                        <TextBlock Grid.Column="3" Text="{Binding LocalAddress}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="4" Text="{Binding LocalPort}" />
                        <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{Binding State}" />
                            <TextBlock Text="{Binding RemoteAddress}" Opacity="0.7" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock x:Name="StatusText" Grid.Row="4" Opacity="0.7" />
    </Grid>
</Page>
