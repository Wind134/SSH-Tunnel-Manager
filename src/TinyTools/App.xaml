
<Application x:Class="TinyTools.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Theme brushes (shared with SSHTunnelManager via DynamicResource) -->
            <SolidColorBrush x:Key="AppBg" Color="WhiteSmoke" />
            <SolidColorBrush x:Key="AppFg" Color="Black" />
            <SolidColorBrush x:Key="PanelBg" Color="#F5F5F5" />
            <SolidColorBrush x:Key="BorderCol" Color="LightGray" />
            <SolidColorBrush x:Key="MutedFg" Color="#555555" />

            <Style TargetType="Button">
                <Setter Property="MinWidth" Value="60" />
                <Setter Property="Cursor" Value="Hand" />
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <!-- Sidebar nav button style -->
            <Style x:Key="NavButton" TargetType="Button">
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Padding" Value="16,10" />
                <Setter Property="Margin" Value="0,2" />
                <Setter Property="BorderThickness" Value="3,0,0,0" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Cursor" Value="Hand" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#E8E8E8" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="NavButtonActive" TargetType="Button" BasedOn="{StaticResource NavButton}">
                <Setter Property="BorderBrush" Value="#1C7ED6" />
                <Setter Property="Background" Value="#E0EDFF" />
                <Setter Property="FontWeight" Value="Bold" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
