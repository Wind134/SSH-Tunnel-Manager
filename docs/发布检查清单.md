# TinyTools 发布检查清单

## 自动检查

- [x] `dotnet test tests/TinyTools.Tests/TinyTools.Tests.csproj -c Release`
- [x] `dotnet build src/TinyTools/TinyTools.csproj -c Release`
- [x] 运行 `build.ps1` 完成测试与发布
- [x] 确认发布目录仅包含 `TinyTools.exe`
- [ ] 推送版本标签后确认 GitHub Actions 测试、打包和 Release 全部成功

## SSH 隧道真机验收

- [ ] 密码认证可建立和停止反向隧道
- [ ] RSA/Ed25519 私钥认证可建立和停止反向隧道
- [ ] 首次连接显示 Host Key，接受后能够持久化
- [ ] 已信任服务器的 Host Key 变化时连接被阻止
- [ ] 本地代理未启动时给出明确日志
- [ ] 远程端口被占用时给出明确日志
- [ ] 断网后按 3、3、3、10、10 秒策略重连，达到上限后停止
- [ ] 退出应用后 SSH 连接和远程转发端口均释放

## 桌面外壳验收

- [ ] 第二个 TinyTools 实例被阻止
- [ ] “启动时最小化到系统托盘”生效
- [ ] “关闭时最小化到托盘”开启和关闭两种行为均正确
- [ ] 托盘双击与“打开”菜单能够恢复窗口
- [ ] 托盘“退出”能够停止隧道并退出
- [ ] 浅色、深色和跟随系统主题均可读
- [ ] 在 SSH 隧道与句柄查看器之间反复切换不会重复加载配置

## 句柄查看器真机验收

- [ ] IPv4/IPv6 监听端口和连接能够刷新、筛选
- [ ] 普通进程能够打开所在目录、复制详情和终止
- [ ] 关键系统进程无法从应用中终止
- [ ] 文件选择和拖放均能查询锁定进程
- [ ] 锁定进程结束后刷新结果正确
- [ ] 普通权限和管理员权限下的访问拒绝行为清晰

## 发布产物

- [ ] 在没有安装 .NET Runtime 的 Windows 10/11 机器启动成功
- [ ] `data` 目录可写，配置保存和备份恢复正常
- [ ] 从旧 `%APPDATA%\SSHTunnelManager\config.json` 自动迁移成功
- [ ] 压缩包不包含真实配置、密钥、PDB 或崩溃日志
- [ ] Release 版本号、标签、压缩包名称和发行说明一致
